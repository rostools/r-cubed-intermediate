1. Use the powerful (but also very difficult) regular expressions to
   process and clean character data by making use of the `{stringr}`
   package.
2. Handle dates and times in R using the `{lubridate}` package.
